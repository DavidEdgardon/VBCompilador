Public Module module1
    public function hipotenusa(lado1 as integer, lado2 as integer) as integer
	dim lado3 as integer
	lado3 = lado1 ^ 2
	dim lado4 as integer 
	lado4 = lado2 ^ 2
	dim sumalados as integer
	sumalados= lado3 + lado4
	dim hipo as integer
	hipo=raiz(sumalados)
	console.writeline("la hipotenusa fue calculada")
	console.writeline(hipo)
    End Function

    sub main()
	hipotenusa(3,4)
    end sub
end module

